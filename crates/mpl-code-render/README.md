# mpl-code-render

Renders source code with line numbers and lightweight point or span annotations.
Positions use zero-based line and Unicode-character offsets; helpers convert
them to and from UTF-8 byte offsets.
